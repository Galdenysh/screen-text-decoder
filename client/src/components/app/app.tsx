import React, { FC } from "react";

const App: FC = () => {
  const text = "Hello";

  return (
    <main>
      <div>
        <p>
          {text}
        </p>
      </div>
    </main>
  );
};

export default App;
